apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: gsm-store
  namespace: workout          # namespace where your app runs
spec:
  provider:
    gcpsm:
      projectID: porfolio-480111
      auth:
        workloadIdentity:
          clusterLocation: us-central1-a
          clusterName: workout-gke
          serviceAccountRef:
            name: external-secrets       # KSA in the ESO namespace
            namespace: external-secrets
