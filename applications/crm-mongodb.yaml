apiVersion: v1
kind: List
metadata:
  name: crm-mongodb-disabled
  annotations:
    note: "Deprecated. MongoDB Community Operator is installed via infra/, and the MongoDBCommunity CR is rendered by the demo-crm Helm chart."
items: []
